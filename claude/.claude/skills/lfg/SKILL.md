---
name: lfg
description: Create a fresh branch from main/master for new work
disable-model-invocation: true
---

Run this exact command. Do not explain, just run it and show the result:

```bash
bash "${CLAUDE_SKILL_DIR}/lfg.sh" $ARGUMENTS
```
